Q81: Count characters in a string without using built-in length functions.

/*
Sample Test Cases:
Input 1:
Hello
Output 1:
5


//Solution:

#include <stdio.h>

int main() {
    char str[100];
    int count = 0;

    // Taking input string
    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);

    // Counting characters manually
    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] != '\n') {  
            count++; // Increase count for each character except newline
        }
    }

    printf("Total number of characters = %d\n", count);

    return 0;
}


Q82: Print each character of a string on a new line.

/*
Sample Test Cases:
Input 1:
Hi
Output 1:
H
i

*/

//Solution:

#include <stdio.h>

int main() {
    char str[100];

    // Taking input string
    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);

    // Printing each character on a new line
    printf("\nCharacters in the string:\n");
    for (int i = 0; str[i] != '\0'; i++) {
        printf("%c\n", str[i]);
    }

    return 0;
}
